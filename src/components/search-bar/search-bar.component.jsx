import React from "react";

import styles from "./search-bar.module.scss";

const SearchBar = () => {
    return (
        <div className={styles.wrapper}>Search bar</div>
    )
}

export default SearchBar;